/**
 * `features/inventory` — backoffice inventory tracking.
 *
 * Each product variant has a single inventory row. The admin adjusts
 * quantity via increase / decrease / set-quantity ops and tracks
 * movements over time.
 */
export {};
