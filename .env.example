# --- Backend REST API ---
# Base URL of the Go backend (no trailing slash).
# Development default points to the local Gin server.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# --- Branding ---
NEXT_PUBLIC_SITE_NAME=ML Cosmetics
